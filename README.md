# CẤU TRÚC THƯ MỤC ĐỂ KIỂM THỬ
# Tạo thư mục để lưu trữ scripts kiểm thử
mkdir backend\tests

# Tạo thư mục để lưu trữ kết quả kiểm thử và các file liên quan
mkdir backend\test-results
mkdir backend\test-results\screenshots
mkdir backend\test-results\pdfs
mkdir backend\test-results\logs